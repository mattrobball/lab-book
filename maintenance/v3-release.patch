diff --git a/.claude-plugin/marketplace.json b/.claude-plugin/marketplace.json
index 19e344d..85eb87e 100644
--- a/.claude-plugin/marketplace.json
+++ b/.claude-plugin/marketplace.json
@@ -10,3 +10,3 @@
       "description": "Lab notebook infrastructure for coding-agent sessions: claims with status, dispatched runs with replayed evidence, an append-only notebook.",
-      "version": "3.0.0-alpha.1"
+      "version": "3.0.0-alpha.2"
     }
diff --git a/.claude-plugin/plugin.json b/.claude-plugin/plugin.json
index 0424d62..30f8a5a 100644
--- a/.claude-plugin/plugin.json
+++ b/.claude-plugin/plugin.json
@@ -3,3 +3,3 @@
   "description": "Lab notebook infrastructure for coding-agent sessions: claims with status, dispatched runs with replayed evidence, an append-only notebook.",
-  "version": "3.0.0-alpha.1",
+  "version": "3.0.0-alpha.2",
   "author": {
diff --git a/.github/workflows/kit-tests.yml b/.github/workflows/kit-tests.yml
index 9b5bd50..820e2db 100644
--- a/.github/workflows/kit-tests.yml
+++ b/.github/workflows/kit-tests.yml
@@ -62,2 +62,11 @@ jobs:
           exit "$result"
+      - name: Actual alpha.1 upgrade and confined canary
+        run: |
+          printf '%s\n' 'python skills/open-lab/integration/upgrade_contract.py' > evidence/upgrade.command
+          set +e
+          python skills/open-lab/integration/upgrade_contract.py > evidence/upgrade.stdout 2> evidence/upgrade.stderr
+          result=$?
+          echo "$result" > evidence/upgrade.exit
+          cat evidence/upgrade.stdout evidence/upgrade.stderr
+          exit "$result"
       - uses: actions/upload-artifact@v4
diff --git a/.github/workflows/v3-jev-sdk.yml b/.github/workflows/v3-jev-sdk.yml
index ba57141..99e3f20 100644
--- a/.github/workflows/v3-jev-sdk.yml
+++ b/.github/workflows/v3-jev-sdk.yml
@@ -3,3 +3,3 @@ on:
   push:
-    branches: ['feature/v3-jev-adapter-*']
+  pull_request:
   workflow_dispatch:
diff --git a/.github/workflows/v3-pilot.yml b/.github/workflows/v3-pilot.yml
index 421b99f..563fb94 100644
--- a/.github/workflows/v3-pilot.yml
+++ b/.github/workflows/v3-pilot.yml
@@ -3,3 +3,3 @@ on:
   push:
-    branches: ['integration/v3-pilot-ci-*', 'feature/v3-host-identity-*']
+  pull_request:
   workflow_dispatch:
@@ -21,3 +21,4 @@ jobs:
           sudo apt-get update -qq
-          sudo apt-get install -y nginx postgresql-client bubblewrap
+          sudo apt-get install -y nginx postgresql-client
+          bash skills/open-lab/integration/prepare-linux-replay.sh
           python -m pip install playwright==1.55.0
diff --git a/skills/open-lab/SKILL.md b/skills/open-lab/SKILL.md
index 4da2ec5..86809a3 100644
--- a/skills/open-lab/SKILL.md
+++ b/skills/open-lab/SKILL.md
@@ -6,3 +6,3 @@ license: MIT
 metadata:
-  version: "3.0.0-alpha.1"
+  version: "3.0.0-alpha.2"
 ---
@@ -108,3 +108,2 @@ Then scaffold the lab repository:
    - `scripts/run.py` → `run.py`
-   - `scripts/replay.py` → `replay.py`
    - `scripts/rectification.py`, `scripts/reservations.py`, `scripts/board.py` → the same names in the lab root
diff --git a/skills/open-lab/assets/v3/replay.py b/skills/open-lab/assets/v3/replay.py
index 35bb16d..c31bb45 100644
--- a/skills/open-lab/assets/v3/replay.py
+++ b/skills/open-lab/assets/v3/replay.py
@@ -142,5 +142,10 @@ def command_line(stage, original, home, tmp, command):
                    '(allow process-exec)', '(allow signal (target self))',
-                   '(allow sysctl-read)', '(allow file-read-metadata)',
+                   '(allow sysctl-read (sysctl-name "hw.machine") (sysctl-name "hw.ncpu")'
+                   ' (sysctl-name "hw.memsize") (sysctl-name "hw.pagesize")'
+                   ' (sysctl-name "hw.optional.arm64") (sysctl-name "kern.ostype")'
+                   ' (sysctl-name "kern.osrelease") (sysctl-name "kern.osversion")'
+                   ' (sysctl-name "kern.argmax"))', '(allow file-read-metadata)',
                    '(allow mach-lookup (global-name "com.apple.system.logger"))',
                    '(allow file-read* ' + ' '.join(sub(p) for p in runtime_paths()) + ')',
+                   '(allow file-map-executable ' + ' '.join(sub(p) for p in runtime_paths() + [stage]) + ')',
                    '(allow file-read* file-write* ' + ' '.join(sub(p) for p in (stage,home,tmp)) + ')',
diff --git a/skills/open-lab/scripts/tests/test_replay_isolation.py b/skills/open-lab/scripts/tests/test_replay_isolation.py
index cd029ef..8f91d35 100644
--- a/skills/open-lab/scripts/tests/test_replay_isolation.py
+++ b/skills/open-lab/scripts/tests/test_replay_isolation.py
@@ -5,2 +5,4 @@ from pathlib import Path
 import socket
+import subprocess
+import sys
 import tempfile
@@ -9,3 +11,3 @@ from unittest.mock import patch
 
-import replay
+from run import confined_replay as replay
 import run
@@ -93,2 +95,28 @@ print('CHECK_OK')
 '''.replace('PATHS',repr(paths)).replace('PORT',str(port))
+        if sys.platform == 'darwin':
+            # Verify the OTHER host process's argument/environment interface is
+            # actually readable outside, then denied inside the sandbox. Use
+            # the platform header rather than hard-coded sysctl numbers.
+            src = packet / 'proc_probe.c'
+            src.write_text("""#include <sys/types.h>
+#include <sys/sysctl.h>
+#include <errno.h>
+#include <stdlib.h>
+#include <stdio.h>
+#include <string.h>
+int main(int argc,char **argv) {
+  if (argc != 3) return 2;
+  int mib[3] = {CTL_KERN, KERN_PROCARGS2, atoi(argv[1])};
+  char buf[262144]; size_t len = sizeof(buf);
+  int result = sysctl(mib,3,buf,&len,NULL,0);
+  int denied = result < 0 && (errno == EPERM || errno == EACCES);
+  if (!strcmp(argv[2],"deny")) { if (!denied) return 3; puts("DENIED host process environment"); }
+  else { if (result) return 4; puts("OUTSIDE process read control"); }
+  return 0;
+}
+""")
+            exe=packet/'proc_probe'
+            subprocess.run(['cc',str(src),'-o',str(exe)],check=True,capture_output=True)
+            subprocess.run([str(exe),str(os.getpid()),'allow'],check=True,capture_output=True)
+            code = "import subprocess\nsubprocess.run(['./packet/proc_probe', %r, 'deny'], check=True)\n" % str(os.getpid()) + code
         (packet/'probe.py').write_text(code)
